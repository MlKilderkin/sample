сть очередь в которой есть какие-то важные обьекты которые надо обрабатывать.

есть класс\метод - Queue::getMessage() - возвращает один обьект. Тут не важно какой обьект что он делает и какую инфу содержит
есть класс\метод - Handler::processMessage($object) - обрабатывает обьект который вернулся из предыдущего вызова Queue:getMessage()

Необходимо написать функцию или метод класса который будет безприрывно работать и обрабатывать очередь с использованием 2х методов описаных выше.
описать и обьяснить как данный скрипт будет запускаться на Unix платформе, какие ресурсы потребуються для запуска.
Описать проблемы или риски такого подхода.  Описать какая нагрузка будет на сервер с таким подходом -
Допустим, что вышеописаные 2 метода потребляют малейшее кол-во оперативной памяти и не дают нагрузку на проц и диск.
PHP интерпритатор уже установлен на данный Unix сервер

Пример:
запустить скрипт из консоли. пустить скрипт в бекграунде что б он не падал при закрытии ssh соединения.
риски - при фатале, ексепшине или сегфолте, скрипт падает и очередь больше не обрабатывается.